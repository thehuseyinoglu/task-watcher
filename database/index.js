const userDatabase = require("./user-database");
const roomDatabase = require("./room-database");
const taskDatabase = require("./task-database");

module.exports = { userDatabase, roomDatabase, taskDatabase };
